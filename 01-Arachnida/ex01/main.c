#include "spider.h"



int main(int argc, char** argv) {

    t_Opts *opts = ft_args(argc, argv);

    // ft_request(opts->url);


    // parse_html(response);
    free(opts);
    // free(response);
    // system("leaks spider");
}